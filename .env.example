# Safe development defaults. Copy to .env only when a local tool requires it.
APP_ENV=local
INTEGRATION_MODE=mock
ALLOW_BILLABLE_REQUESTS=false
SIMULATION_ENABLED=true
SIMULATION_SCENARIO_SEED=rio-claro-local

SUPABASE_TARGET=local
MAP_PROVIDER=mock
NAVIGATION_PROVIDER=simulated
IDENTITY_PROVIDER=mock
SIGNATURE_PROVIDER=mock
PAYMENT_PROVIDER=mock
NOTIFICATION_PROVIDER=local
ADS_PROVIDER=mock
